# SESSION — текущее состояние разработки

> Обновлено: 2026-06-30

## Активная ветка

- `main` — **UI-правки дизайна** (разделы 1–7, поэтапно)

---

**В работе (2026-06-30) — индикаторы прогресса**

**Готово (шаг 2):** `showProgress`, `showStepProgress`, `showRingProgress`, `fakeProgress` + CSS v2.

**Готово (шаг 4, группа 1):** линейный прогресс для отзывов/вопросов — load/generate/send/template + resume.

**Готово (шаг 4, группа 2):** линейный прогресс для чатов WB и Ozon — список, переписка, массовый автоответ.

**Готово (шаг 4, группа 3):** линейный прогресс для акций Ozon — загрузка списка, sync-discount, удаление, автоудаление.

**Готово (шаг 4, группа 4):** пошаговый прогресс мастера связок — `showStepProgress` на шагах 1–6.

**Готово (шаг 4, группа 5):** кольцевой прогресс — автозапуск «Запустить сейчас», скан/перескан алертов Ozon.

**Шаг 4 завершён.** Не мигрировано (низкий приоритет): каталог/ИИ связок (`#card-links-loading`), скан WB-алертов.

**Документация:** в AI_CONTEXT, WORKFLOW и ARCHITECTURE добавлено обязательное правило «Прогресс для долгих операций».

---

## В работе (2026-06-30) — UI-редизайн страниц

**Готово (разделы 1–7):** фильтры отзывов/вопросов, empty-state, вкладки и степпер связок, switch-toggle, карточки магазинов, вкладки настроек.

---

## Последнее (2026-06-18) — Выбор магазинов в автозапуске по площадкам

**Готово:** в «Автозапуск» — отдельные списки WB / ЯМ / Ozon и отдельно для акций Ozon.
В «Акции Ozon → Настройки» — блок «Автозапуск: магазины для синхронизации акций».
Пустой список площадки — берутся магазины из общего списка.

---

## (2026-06-18) — WB: дедупликация новостей между магазинами

**Готово:** `news_id` портала WB общий для всех ЛК — повторный ИИ, запись и Telegram для других магазинов не выполняются.
В списке «Все магазины» каждая новость показывается один раз.

---

## (2026-06-18) — WB: дата начала выборки новостей

**Готово:** поле «Новости с даты» на панели «Уведомления» (для WB) и в «Настройки → WB»; синхронизируются.
Пусто — последние 30 дней. В ответе scan — `wb_alert_from_date`.

---

## (2026-06-18) — WB: ИИ + Telegram для новостей

**Готово:** отдельный промпт `wb_important_alert` в «Настройки → Промпты».
После фильтра заказов/отмен ИИ классифицирует новости; важные — в раздел «Уведомления» и в Telegram (шаблон + chat_id в «Настройки → Telegram»).

---

## (2026-06-18) — Уведомления WB в разделе «Уведомления»

**Готово:** загрузка новостей портала WB (`common-api` / `communications/v2/news`).
Фильтр: скрываются новые заказы и уведомления о скорой отмене.
Настройки: «Настройки → WB», автозапуск: галочка «Уведомления WB».

---

**Готово:** KPI-карточки на сводке видны сразу; блок «Очередь и автозапуск» всегда открыт.
Настройки акций Ozon — крупные переключатели с пояснениями, карточки действий.

---

**Готово:** кнопка внизу сайдбара разворачивает меню с подписями разделов; состояние сохраняется в `localStorage`.

---

**Готово:** новый `login.html` в стиле MarketAI v2.
- Canvas-сетка частиц с линиями и «дыханием»
- Логотип M: пульсирующие кольца, вращающая рамка, квадратики от иконки
- Форма: stagger slide-up; кнопка «Войти» — световая развёртка + стрелка вправо
- Два фоновых свечения; палитра `#7F77DD`
- Кот-маскот убран; логика `/api/auth/login` без изменений

---

**Готово:** полный редизайн по `marketai_dashboard_v2.html` (шаги 1–11).
- KPI `.stat-card` — плоские карточки, `.label`/`.value` + `.stat-badge`
- Таблицы `.items-table`, `.ops-log-table` — surface, 12px radius, uppercase th, hover
- Кнопки — flat `#7F77DD`, 6px radius, 13px
- Формы — focus ring `var(--accent-bg)`
- Тёмная тема — палитра `#0d0d0d` / `#141414` / `#222`
- Cleanup — watermarks/bg скрыты, `.app-nav` скрыт, box-shadow убраны (кроме focus), scrollbar

**Ранее (шаги 1–5):** Tabler Icons, CSS-токены, sidebar 52px, layout, panel-toprow.

---

## Последнее (2026-06-18) — Ozon акции: синхронизация по порогу скидки

**Задача:** вместо только «автоудаления» — полная синхронизация всех акций по порогу (по умолчанию 3%): ≤ порога оставить/добавить, > порога снять. SKU с нулевыми/странными ценами — пропуск с записью в отчёт.

**Сделано (локально):**
- `app/core/ozon_actions.py` — `sync_actions_by_discount_threshold()`, расчёт скидки, пропуски с причинами
- `app/core/ozon_client.py` — `list_auto_add_products`, `delete_auto_add_products`, нормализация activate
- `app/core/workflows.py` — `ozon_actions_sync_discount_for_store()`
- `app/web/server.py` — настройки, `POST /sync-discount`, автозапуск по `sync_mode`
- UI: вкладка «Акции Ozon» — порог, режим, кнопка «Синхронизировать по порогу»
- Telegram-отчёт и audit: −/+, оставлено, пропусков данных
- `config_backup` — экспорт/импорт новых полей настроек

**Автозапуск:** галочка «Синхронизация акций Ozon» → `discount_threshold` или `legacy_auto_remove` по настройкам магазина.

**Следующий шаг:** деплой, проверка на реальном магазине Ozon, commit по запросу.

---

## Последнее (2026-06-20) — Мастер: Internal error при загрузке WB

**Симптом:** alert с `{"errorText":"Internal error"…}` при шаге 1 «Загрузить WB» у отдельного магазина.

**Причина:** ответ WB Content API (часто — неверный/просроченный токен категории «Контент» у этого магазина); в UI показывался сырой JSON.

**Исправление:** человекочитаемые ошибки, ретраи страниц, частичная загрузка; проверить API-ключ магазина в «Магазины».

---

## Последнее (2026-06-20) — Мастер шаг 4: классификация косметики/запчастей

**Проблема:** ИИ плохо различал типы в больших subject (до 80 SKU × 120 символов title); карточки после 80-й не классифицировались; много fallback «косметика» → смешанные связки (ISANA гели, дезодоранты, маски).

**Исправление:**
- Расширены rule-паттерны в `card_links.py` (дезодорант, маска, глаза, воск, лак, …)
- ИИ: батчи по 20, subject+бренд, сжатый title, отдельные промпты; все SKU обрабатываются чанками
- После деплоя: шаги 1→4→5 заново на магазине (кэш classify/plan пересчитать)

---

## Последнее (2026-06-20) — Автосвязка: перепроверка subjectID + модель для запчастей

**Проблема:** ошибка «Разные предметы WB (subjectID)» — в одну пачку попадали аккумуляторы Vivo и Asus (одна категория «Аккумуляторы…», бренд «Нет бренда»).

**Исправление** (`app.js`, только автосвязка):
- Группировка: категория + бренд + subject_id; для запчастей — дополнительно по модели из названия (Samsung→Samsung и т.д.)
- Без subject_id запчасти не смешиваются — одиночки или группа по модели
- При ошибке subjectID — перепроверка: разбивка пачки по subject/модели и повторная отправка под-пачек

---

## Последнее (2026-06-20) — Fix plan NameError

**Ошибка:** `card_links_master step plan failed: name 'out' is not defined`  
**Причина:** опечатка в конце `master_step_plan` — `return out` вместо `return rows, bundles_out, meta`.  
**Исправлено** в `card_links_master.py`.

---

## Последнее (2026-06-20) — Fix сброс кэша при повторной загрузке

**Проблема:** при повторном шаге 1 «Загрузить WB» в журнале оставалась строка «Загружено 10000 карточек», шаги 2–5 считались выполненными.

**Причина:** `clm_clear_store` удалял только items/bundles, не state (steps, log).

**Исправление:** полный сброс state; шаги обнуляются; UI очищает лог сразу при старте загрузки.

---

## Handoff (2026-06-19) — Мастер связок WB

### Сделано

- **Новый модуль** `app/core/card_links_master.py`: 6 независимых шагов, кэш в SQLite
- **План:** IKEA (subject), дом (subject+brand), косметика (+type, фасовки 1/2/3), запчасти (модель); max **29**
- **Apply:** `disconnect_first`, пропуск уже в целевой imtID, ошибки WB/валидации в лог, store lock
- **API:** status / bundles / bundle-ids / step → фоновая задача
- **UI:** вкладка «Мастер связок», фильтры, пагинация, выбрать все, **объединить выбранные**, apply
- Категория в UI — **только WB** subject/parent (как в ИИ-связках)

### Файлы

| Файл | Изменение |
|------|-----------|
| `app/core/card_links_master.py` | новый пайплайн |
| `app/db.py` | таблицы + `clm_*` |
| `app/web/tasks.py`, `server.py` | задачи + REST |
| `app/web/store_locks.py` | op `card_links` |
| `app/web/static/card_links_master.js` | UI мастера |
| `index.html`, `app.js`, `styles.css` | вкладка + стили |

### Риски / проверить на production

- ~~Plan: `_row_category_label(chunk)` — исправлено (`chunk[0]`)~~
- Cross-brand merge мелких пачек в одном subject (дом) — может отклонить WB
- OpenAI на шаге 4 опционален (правила + `unknown` для моделей)
- После apply каталог WB не перезагружается автоматически — шаг 1 вручную
- Изменения **не закоммичены** — commit/push по запросу

### Следующий шаг

1. Деплой → магазин WB → шаги 1–5 → проверить план на ~12k SKU
2. Apply 1–2 связки → сверить imtID в кабинете WB
3. Закоммитить по запросу

---

## Handoff (2026-06-18) — Bin-pack: размечай → упаковывай (шаг 1)

### Проблема (из CARD_LINKING_REVIEW.md)

Pack-шаг финализировал фасовки 1/2/3 и **выкидывал** артикулы из `ai_pool` → сиротские мини-связки. Группировщик `_merge_ai_suggestions_by_use_bucket` работал только над кандидатами ИИ с гейтом `len(cluster) < 2`.

### Сделано

- **`deterministic_bin_pack_from_pool`** — первичная упаковка **всего пула** по `(subjectID, бренд, назначение)` до 30
- **`_facet_aware_pack_bins`** — атомы фасовок целиком, мягкая сортировка по title_base (линейка/вкус — приоритет, не жёсткий split)
- **`ai_pool = pool`** — pack-артикулы **не изымаются** из пула ИИ
- Старый ранний `deterministic_pack_suggestions` в пайплайне заменён на bin-pack
- `_merge_ai_suggestions_by_use_bucket`: убран гейт `< 2`, facet-aware split; ИИ-чанки, уже покрытые bin-pack, пропускаются
- **`default_ai_system_prompt`**: ИИ — дополнение к bin-pack, не дублировать бренд+назначение; мягкий приоритет линейки при добивке

### Следующие шаги (по документу)

1. LLM-разметка «назначения» для категорий вне регэкспа (не-косметика)
2. Упростить промт → разметчик фасетов
3. Метрика: сравнить `uncovered` до/после на реальном каталоге

### Handoff (2026-06-18) — покрытие: 5300 «без предложения»

**Причина:** `bin_covered` глушил ИИ для уже оптимально связанных товаров; bin-pack только по косметическим use-bucket.

**Исправлено:** `brand_general` (subject+бренд), balm/cream_unspecified; `bin_covered` только из реальных предложений; статус «уже в норме» / «без связки».

### Handoff (2026-06-18) — Точечная развязка перед перепривязкой

**Запрос:** при применении связки развязывать только товары, которые переносим — не весь каталог.

**Сделано:**
- `wb_merge_cards(disconnect_first=True)` — перед merge развязывает только linked nm_id с чужим imtID; батчи по 30
- `wb_disconnect_cards` — батчи >30
- `ozon_link_by_model(unlink_first=True)` — развязка offer_id с другой моделью перед link
- API: `disconnect_first` / `unlink_first`; UI: подтверждение «Сначала развязать N…»

### Handoff (2026-06-19) — «Применено 0 из 2» на ИИ-связках

**Причина:** «Новая связка» с уже заданным imtID отправляла все nm_id, хотя товары уже в целевой связке → WB «уже в этой связке».

**Исправлено:** фильтрация moving nm_id, skip связок без переноса, new_link→merge_groups при известном imtID, текст ошибки в bulk-тосте.

### Handoff (2026-06-19) — Ложный «лимит 30» и категория щеток

**«Лимит 30» на 2–3 товарах:** товары уже в целевой imtID → UI показывает «уже в связке», не «лимит 30».

**Категория в UI:** только subject/parent из WB (без подмены по названию). Щётки под «Губки…» — если так в каталоге WB.

**Hotfix:** `da3c9dc` — убран забытый вызов `_items_display_category_label` (NameError в `card_links_ai`).

---

### Проблема

1. «Перепривязать — ровно один товар» при применении связки с 3+ товарами (kind `relocate` с несколькими items)
2. Нельзя выбрать несколько связок для «Применить выбранные» (путаница со «Склейка», жёсткий min 2 moving)
3. Нет выбора размера страницы (только 100)

### Исправлено

- Backend `_merged_apply_candidate`: `relocate`/`attach` с >1 товаром → `merge_groups`
- Frontend `cardLinksNormalizeApplyCandidate` + ослаблен `cardLinksCanBulkApplyBundle` (1+ товар для merge/relocate)
- Галочка слева — для массового применения; «Склейка 2» — только объединение двух связок
- Селектор «На странице»: 50 / 100 / 200 / Все (localStorage)
- **Лимит 30:** не считаем повторно товары уже в целевой связке; oversized связки режутся на части

---

### Проблема

use-merge склеивал все бальзамы subjectID вместе (Labello + Balea + SUNDANCE). Блески lavera 03/04 — в разных связках.

### Исправлено

- Склейка по назначению только внутри **одного бренда**
- `_row_brand_key`: Balea/Labello/lavera из «Гигиеническая помада Balea…»
- Валидатор: разные бренды → отклонение
- «Жидкая помада» / категория «Блески» → lipstick bucket
- Промпт: один бренд на связку; оттенки lavera вместе

---

### Проблема

«Гигиеническая помада» попадала в bucket `lipstick`, дробилась по вкусам; фасовки 1/2/3/5 — баг в ключе «4.8 г».

### Исправлено

- `гигиеническая помада` → `lips` (бальзам для губ, не цветная помада)
- Склейка `lips` + `lipstick` → `lip_care` в одном subjectID
- `_title_base_key`: не съедает «4» из «4.8 г»
- Склейка по subjectID (требование WB API при merge imtID)
- Промпт: все гигиенические помады разных вкусов вместе

---

## Handoff (2026-06-18) — UI вкладки ИИ (колхоз)

### Исправлено

- Пагинация: `[hidden]` не работал из-за `display:flex` — стрелки без текста на пустом экране
- Фильтры свёрнуты в `<details>`, на вкладке ИИ закрыты по умолчанию
- Панель ИИ: текст отдельно, кнопки отдельно; пагинация внутри панели
- Убран дубль подсказки в таблице; скрыт thead каталога на вкладке ИИ
- `app.js?v=60`

---

## Handoff (2026-06-18) — пагинация и склейка ИИ-связок

### Сделано

- Пагинация результатов ИИ: ~100 товаров на страницу (меньше лагов DOM)
- Сортировка связок: категория → название (backend + frontend)
- «Склейка» — выбор 2 связок в одной категории, кнопка «Склеить 2 связки»
- «Применить связку» — связка сразу уходит со списка, без ожидания ответа API
- `app.js?v=59`

### Следующий шаг

- Проверить на большом каталоге после деплоя

---

## Handoff (2026-06-18) — редактор промпта ИИ-связок

### Сделано

- В «Настройки ИИ-связок» — textarea системного промпта (WB/Ozon отдельно)
- API: GET/PUT `/api/card-links/ai-prompt/{marketplace}`
- Промпт в `app_settings`; при запуске ИИ берётся из БД
- Кнопки: «Сохранить промпт», «Вернуть встроенный»
- Плейсхолдер `{max_link_items}` → 30
- `app.js?v=57`

### Следующий шаг

- Задеплоить и проверить сохранение/сброс на production

---

## Handoff (2026-06-18) — промпт ИИ связок v2

### Сделано

- Уточнён system prompt: лимит 30, набор связок 30+12, без мелких обрывков, без brand-merge 300 SKU

---

## Handoff (2026-06-18) — откат brand-merge (332 крема Balea)

### Проблема

Слияние «весь бренд в категории» склеило 332 крема Balea в одну связку.

### Сделано

- Удалено `_merge_ai_suggestions_by_brand_category`
- Остаётся только: **1/2/3 шт одной линейки** в одной связке
- UI: если >30 — автоматически части по 30
- `app.js?v=56`

---

## Handoff (2026-06-18) — фасовки 1/2/3 всегда вместе

### Сделано

- Главное правило: **1/2/3 шт одного товара — одна связка**, даже в разных imtID
- `merge_groups` в авто-фасовках при разных imtID
- `_merge_ai_suggestions_by_pack_key` — склеивает разъехавшиеся фасовки до brand-merge

---

## Handoff (2026-06-18) — слияние по бренду в категории

### Проблема

ИИ/pack давали отдельную связку на каждую линейку (3× Balea scrub, 3× ARTDECO тени) вместо одной.

### Сделано

- `_merge_ai_suggestions_by_brand_category`: один бренд + категория → одна связка (до 30)
- Промпт ИИ: правило 11 — не дробить SKU одного бренда
- Заголовок связки: «Balea · imtID …»
- `app.js?v=55`

---

## Handoff (2026-06-18) — ИИ по категориям целиком

### Сделано

- **Один запрос OpenAI на категорию** (все товары subject_id/category_key вместе)
- Резка только если категория &gt; лимита (60–150 в настройках)
- В промпт: название категории + product_count
- Лимит «Макс. категорий за запуск» вместо «запросов»
- `app.js?v=54`

---

## Handoff (2026-06-18) — процент прогресса + ускорение ИИ

### Сделано

- Полоска прогресса с **процентом** (determinate bar + «42%» справа)
- OpenAI-батчи **параллельно до 3** (≈3× быстрее при большом каталоге)
- Батч по умолчанию **60** товаров (меньше запросов)
- `app.js?v=53`, `styles.css?v=29`

---

## Handoff (2026-06-18) — таймаут ИИ / фоновая задача

### Проблема

- ИИ-связки держали один HTTP-запрос на 10+ мин (много последовательных вызовов OpenAI, не WB)
- Render/прокси обрывал соединение → «долгое ожидание», UI мог зависнуть под overlay «коровы»

### Сделано

- POST `ai-suggest` → `{ task_id }`, работа в фоне, опрос `/api/tasks/{id}` с прогрессом
- Лимит `max_ai_batches` (по умолчанию 12), приоритет крупных категорий
- Настройка «Макс. запросов к ИИ за запуск» в UI
- Снятие overlay при ошибке/старте ИИ
- `app.js?v=52`

### Следующий шаг

Деплой → обновить страницу → загрузить каталог → ИИ → смотреть прогресс «запрос N/M»

---

## Handoff (2026-06-18) — итоговые связки ИИ + весь каталог

### Сделано

- UI ИИ: **итоговые связки** (все товары в связке после применения), не отдельные операции
- Метки: остаётся / перенести / добавить; заголовок с imtID или моделью
- По умолчанию анализ **всего загруженного каталога** (лимит 400 убран)
- Статус: проверено N/M товаров, сколько без предложения
- `app.js?v=51`, `styles.css?v=28`

### Следующий шаг

Обновить страницу → загрузить полный каталог (достаточно страниц) → ИИ → проверить итоговые связки

---

## Handoff (2026-06-18) — ИИ-связки с редактированием

### Сделано

- Вкладка **ИИ**: анализ всего каталога включая уже связанные товары
- Настройки: охват, батч, макс. товаров, авто-фасовки 1/2/3 шт, деление >30
- Редактирование: убрать/добавить товар, название модели, целевая связка
- Выбор пачками + «Применить выбранные» (без автоперезагрузки)
- Backend: батчи по категориям, валидатор, deterministic pack clusters
- `app.js?v=50`, `styles.css?v=27`

### Следующий шаг

Загрузить каталог → вкладка ИИ → «Запустить ИИ» → править → «Применить» → «Обновить»

---

## Handoff (2026-06-18) — ручные связки без автоперезагрузки

### Сделано

- Убрана вкладка «Предложения» → **Справка** (гайд по рассортировке: категории, названия, пример шампуней)
- **Перепроверка** только вручную: кнопка «Запустить перепроверку», без авто-reload после операций
- Кнопка **«Обновить»** для ручного обновления каталога после связки
- Снят запрет на объединение разных брендов (frontend + backend)
- Каталог: галочка **на всю связку** (bundle checkbox)
- Загрузка каталога без тяжёлых эвристик (`suggestions=none` по умолчанию)
- `app.js?v=49`, `styles.css?v=26`

### Следующий шаг

Загрузить каталог → Справка/фильтры → Каталог → выбрать товары или целую связку → «Связать» → «Обновить»

---

## Handoff (2026-06-18) — фильтры бренда/категории

### Сделано

- Панель фильтров после загрузки каталога (бренд, категория, скрытие «мусорных» категорий)
- Фильтры применяются к предложениям, перепроверке и каталогу
- Ручная связка: проверка одного бренда + категории, лимит 30
- `app.js?v=47`

### Следующий шаг

Загрузить каталог → выбрать бренд → «Только без связки» → вкладка «Каталог» → выбрать товары → «Связать»

---

## Handoff (2026-06-18) — список артикулов в Связках

### Сделано

- Режим «Только по списку артикулов продавца»: чекбокс, textarea, загрузка из `.txt/.csv`
- API `articles_only=1` — каталог и предложения только внутри списка
- Сохранение списка и режима в localStorage
- `app.js?v=46`, `styles.css?v=24`

### Осталось

- Ручная проверка WB/Ozon с реальным списком артикулов
- Закоммитить и задеплоить по запросу

### Следующий шаг

Связки → включить режим списка → вставить/загрузить артикулы → «Загрузить»

---

## Handoff (2026-06-18) — WB каталог >1000 карточек

### Сделано

- Исправлена пагинация WB Content API (`list_cards_all`): остановка по `len(batch) < 100`, дедуп nmID, reuse сессии
- Ускорены эвристики `suggest_link_candidates` / `attach` / `review` на больших каталогах (раньше O(n²) давал минуты)
- API каталога WB: таймаут 10 мин; фронт — `timeoutMs: 600000`
- `app.js?v=45`

### Осталось

- Ручная проверка на магазине с >1000 карточек (WB)
- Закоммитить по запросу пользователя

### Следующий шаг

`python3 run_web.py` → Связки → WB → магазин с большим каталогом → «Загрузить»

---

## Handoff (2026-06-16) — корова SVG

### Сделано

- Экран «Нет соединения»: CSS-корова заменена на SVG (гольштинка, Wi‑Fi с крестом, анимация)
- `fx-widgets.css?v=5`

---

## Handoff (2026-06-16) — loading убран

### Сделано

- Удалена анимация бегущей точки в progress-барах (`fx-dot-loading`)
- Прогресс снова обычным текстом (`textContent`)
- `fx-widgets.css?v=4`, `fx-widgets.js?v=4`, `app.js?v=44`

---

## Handoff (2026-06-16) — лампа и логин

### Сделано

- Лампочка перенесена в `header-right` (SVG, компактный шнур) — без плавающего виджета справа/снизу
- Свечи на странице логина удалены; вход снова сразу через POST `/api/auth/login`
- `fx-widgets.css?v=3`, `fx-widgets.js?v=3`

---

## Handoff (2026-06-16) — fx-widgets

### Сделано

- Лампочка: шнур переключает `theme-dark` (синхрон с настройками)
- Корова: полноэкран при потере соединения (Failed to fetch), кнопки Повторить / Закрыть
- Loading: точка по тексту в progress-барах

### Файлы

- `app/web/static/fx-widgets.css`, `fx-widgets.js`
- `index.html`, `app.js?v=42`

---

## Handoff (2026-06-16) — аудит и исправление UI

### Сделано

- Восстановлены отступы в `server.py` (SyntaxError блокировал запуск сервера)
- Сохранён `sort_catalog_rows` в API каталога card-links (WB/Ozon)
- `app.js`: dev-журнал `join('\n')`, импорт настроек обновляет магазины на активной вкладке, маска секретов после сохранения (`loadSettings`), фильтр `log-level`, скрытие Dev без `view_log`, подсветка вкладок card-links
- `styles.css`: `.card-links-catalog-filter[hidden]`
- Кэш-бастинг: `app.js?v=41`, `styles.css?v=23`

### Осталось

- Закоммитить изменения по запросу пользователя
- Верификация на production (card-links, журнал Dev)
- T-000b: закоммитить `docs/` если ещё не в git

### Следующий шаг

Ручная проверка: запуск `python3 run_web.py`, журнал Dev, импорт настроек на вкладке «Магазины», сохранение OpenAI-ключа.

---

## Handoff (2026-06-06) — аудит документации

### Сделано

- Прочитаны все 8 файлов `docs/` и сверен код (`server.py`, `core/*`, `agent/*`, `db.py`, static UI)
- Исправлены несоответствия: сломанная структура каталогов в PROJECT.md, устаревшие оценки строк, пропущенные API и модули
- Созданы **API.md** (справочник ~70 endpoints) и **ENV.md** (env + ключи `app_settings`)
- Обновлены: PROJECT, ARCHITECTURE, AI_CONTEXT, WORKFLOW, CHANGELOG, TASKS, BUGS, SESSION
- Код приложения **не изменялся**

### Осталось

- Закоммитить `docs/` в git (T-000b)
- Верификация card-links на production после `73aefbc`
- Обновить корневой `README.md` (T-004) — вне scope аудита docs
- Диагностика «пропущенных» одиночек (нужны nmID от пользователя)

### Следующий рекомендуемый шаг

1. Закоммитить обновлённую документацию `docs/`
2. Новому разработчику: читать PROJECT → ARCHITECTURE → API/ENV → AI_CONTEXT → SESSION → TASKS

---

## Недавняя работа по коду (контекст)

### Card Links (`6551696` … `73aefbc`)

- Пулы attach, чекбоксы, категории, сортировка каталога
- Fix `group_attach_suggestions`, progress bar, attach matching

### Безопасность (`5e443b9`)

- `secret_mask.py`, write-only ключи, `ADMIN_RESET_TOKEN`

### Деплой

- Render (основной), Timeweb VPS (скрипты в `deploy/timeweb/`)

## Открытые вопросы (production)

| Проблема | Статус на `73aefbc` |
|----------|---------------------|
| Card-links UI после деплоя | Требует верификации |
| Одиночки не в предложениях | Частично исправлено; нужны примеры |

## Как продолжить в новом чате

1. Прочитать по порядку: **PROJECT → ARCHITECTURE → AI_CONTEXT → SESSION → TASKS** ([WORKFLOW.md](./WORKFLOW.md))
2. Справочники: [API.md](./API.md), [ENV.md](./ENV.md)
3. `git log -5 --oneline` и `git status`
4. Следовать workflow: план перед кодом, обновление docs после кода
5. Не пушить без явного запроса
